# Minimal Airflow overrides — copy to airflow.cfg or set as env vars
[core]
executor = LocalExecutor
load_examples = False
dags_are_paused_at_creation = True

[database]
sql_alchemy_conn = postgresql+psycopg2://airflow:airflow@localhost/airflow

[webserver]
expose_config = True
